name: fleet_status
description: "Get fleet status: connectivity, HEAD commit, branch, and dirty
  state for each host."
enabled: true
builtIn: true
category: fleet
schema:
  hosts:
    type: array
    description: Subset of host names to target (defaults to all)
    optional: true
    items:
      type: string
createdAt: 2024-01-01T00:00:00.000Z
