name: fleet_sync
description: Sync a skill from local repository to remote hosts.
enabled: true
builtIn: true
category: fleet
schema:
  skill:
    type: string
    description: Name of the skill directory to sync
    optional: false
  hosts:
    type: array
    description: Subset of host names to target (defaults to all)
    optional: true
    items:
      type: string
createdAt: 2024-01-01T00:00:00.000Z
